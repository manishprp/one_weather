import 'package:flutter/material.dart';

class DataSeparationWidget extends StatelessWidget {
  const DataSeparationWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(
        border: Border.all(
          color: Colors.white10,
          width: 1,
        ),
      ),
    );
  }
}
